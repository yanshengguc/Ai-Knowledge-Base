<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yansheng.aiknowledgebase.mapper.UserMapper">

    <select id="findById"
            resultType="com.yansheng.aiknowledgebase.entity.UserEntity">

        select id, username, password
        from user
        where id = #{id}

    </select>



    <select id="getUserByName" resultType="com.yansheng.aiknowledgebase.entity.UserEntity">
select
        id,
        username,
        password,
        nickname
        FROM user
        where username=#{username}
    </select>

    <insert id="insert">
        insert into user(
        username,
        password,
        nickname
        )
        values(
        #{username},
        #{password},
        #{nickname}
        )

    </insert>


</mapper>