<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yansheng.aiknowledgebase.mapper.ChunkMapper">

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge_chunk
        (
        file_id,
        chunk_index,
        content,
        content_length,
        create_time,
        update_time
        )
        VALUES

        <foreach collection="list"
                 item="chunk"
                 separator=",">

            (
            #{chunk.fileId},
            #{chunk.chunkIndex},
            #{chunk.content},
            #{chunk.contentLength},
            #{chunk.createTime},
            #{chunk.updateTime}
            )

        </foreach>

    </insert>

    <select id="selectByFileId"
            resultType="com.yansheng.aiknowledgebase.entity.ChunkEntity">

        SELECT
        id,
        file_id,
        chunk_index,
        content,
        content_length,
        create_time,
        update_time
        FROM knowledge_chunk
        WHERE file_id = #{fileId}
        ORDER BY chunk_index

    </select>
</mapper>