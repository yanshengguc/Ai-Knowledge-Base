<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yansheng.aiknowledgebase.mapper.KnowledgeMapper">

    <select id="selectAll"
            resultType="com.yansheng.aiknowledgebase.entity.KnowledgeEntity">

        select
        id,
        title,
        content,
        category,
        author,
        create_time,
        update_time
        from knowledge

    </select>


    <select id="selectById"
            resultType="com.yansheng.aiknowledgebase.entity.KnowledgeEntity">

        select
        id,
        title,
        content,
        category,
        author,
        create_time,
        update_time
        from knowledge
        where id = #{id}

    </select>

    <!--新增-->
    <insert id="insert">
        insert into knowledge(
        title,
        content,
        category,
        author,
        create_time,
        update_time
        )
        values(
        #{title},
        #{content},
        #{category},
        #{author},
        #{createTime},
        #{updateTime})
    </insert>
</mapper>